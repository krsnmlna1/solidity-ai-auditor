
## Setup

1. Create a `.env` file in the project root.
2. Add your key:

```env
GOOGLE_API_KEY=your_api_key_here
```
3. Intall venv and activate.
```
python -m venv venv
venv/Scripts/activate
```
3. Install dependencies and run:

```bash
pip install -r requirements.txt
py main.py
```
